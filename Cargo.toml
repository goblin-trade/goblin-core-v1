cargo-features = ["panic-immediate-abort"]

[workspace]
resolver = "2"
members = [
    "goblin-core", "goblin-hostio", "goblin-macros", "goblin-program",
    "goblin-scripts-rs"
]

[profile.release]
opt-level = "s"
strip = true
lto = true
#panic = "abort"
panic = "immediate-abort"
